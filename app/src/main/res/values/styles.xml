<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="LargeTextView">
        <item name="android:textSize">@dimen/LargeText</item>
        <item name="android:text">@string/TextView1</item>
        <item name="android:background">@color/red</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="MediumTextView">
        <item name="android:textSize">@dimen/MediumText</item>
        <item name="android:text">@string/TextView2</item>
        <item name="android:background">@color/yellow</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="SmallTextView">
        <item name="android:textSize">@dimen/SmallText</item>
        <item name="android:text">@string/TextView3</item>
        <item name="android:background">@color/green</item>
        <item name="android:textColor">@color/white</item>
    </style>

</resources>
